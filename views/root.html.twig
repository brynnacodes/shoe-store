<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Shoefinder</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <link rel="stylesheet" href="css/styles.css" type="text/css">
    </head>
    <body>
        <div class="container">
          <div class="jumbotron">
            <h1>Shoefinder: find your next pair!</h1>
          </div>

          <h3>Store Listings</h3>
            <ul>
                {% if stores is not empty%}
                    {% for store in stores %}
                        <a href="/stores/{{store.getId}}"><li>{{ store.getName }}</li></a>
                    {% endfor %}
                {% endif %}
            </ul>

            <h3>Add a Store</h3>
            <form class="" action="/add_store" method="post">
                <label for="name">Add store name</label>
                <input type="text" name="name">
                <button type="submit" name="button">Add store</button>
            </form>

            <h3>Brand Listings</h3>
              <ul>
                  {% if brands is not empty%}
                      {% for brand in brands %}
                          <a href="/brands/{{brand.getId}}"><li>{{ brand.getName }}</li></a>
                      {% endfor %}
                  {% endif %}
              </ul>

              <h3>Add a Brand</h3>
              <form class="" action="/add_brand" method="post">
                  <label for="name">Add brand name</label>
                  <input type="text" name="name">
                  <button type="submit" name="button">Add brand</button>
              </form>
        </div>
    </body>
</html>
